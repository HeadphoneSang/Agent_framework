## 可用的工具  
{tools_description}  
## 工具调用格式 (当你需要调用工具时，请严格遵守以下回答格式)

[TOOL_CALL]tool_name(param0_name="value0", param1_name="value1",...,paramN_name="valueN")

## 工具调用请求的示例

[TOOL_CALL]search(query="中国美食",deep="1")

## 调用工具请严格遵守一下准则:
1. ***调用工具时，每个工具调用只占一行***  
2. 调用工具，仔细阅读可用工具列表，确保参数输入正确
3. 请检查历史会话内容后，再决定调用哪些工具, __切勿重复调用无用的工具！！__

## 提示:
调用工具的结果会自动追加到对话历史中，请基于已有的历史会话内容，结合工具调用结果，给出最合适的答案。
